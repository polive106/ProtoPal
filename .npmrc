; Use hoisted linking only for the mobile package (Metro requires flat node_modules)
; This is set at root level because pnpm doesn't support per-package .npmrc
node-linker=hoisted
strict-peer-dependencies=true
